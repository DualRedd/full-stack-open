{
  "name": "phonebook",
  "version": "1.0.0",
  "type": "module",
  "description": "Full Stack Open Demo Phonebook",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "rm -rf dist && cd ../phonebook_frontend && npm run build && cp -r dist ../phonebook_backend/"
  },
  "author": "Jonni Haapiainen",
  "license": "ISC",
  "dependencies": {
    "express": "^5.2.1",
    "morgan": "^1.10.1"
  },
  "engines": {
    "node": "22.x"
  }
}
